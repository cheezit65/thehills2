<%= form_for(@web_customer) do |f| %>
  <% if @web_customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@web_customer.errors.count, "error") %> prohibited this web_customer from being saved:</h2>

      <ul>
      <% @web_customer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
