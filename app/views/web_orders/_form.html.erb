<%= form_for(@web_order) do |f| %>
  <% if @web_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@web_order.errors.count, "error") %> prohibited this web_order from being saved:</h2>

      <ul>
      <% @web_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
