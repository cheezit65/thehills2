<%= form_for(@web_order_detail) do |f| %>
  <% if @web_order_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@web_order_detail.errors.count, "error") %> prohibited this web_order_detail from being saved:</h2>

      <ul>
      <% @web_order_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
